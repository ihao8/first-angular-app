const assert = require('assert')

it('should return true', () => {
  assert.equal(true, true)
});


it('should return true', () => {
  assert.equal(1, 1)
});
// .then(null, () => {console.log('something wrong')})
